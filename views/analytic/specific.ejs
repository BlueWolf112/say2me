<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="/public/res/bootstrap@5.0.0-alpha2.min.css">
	<title>Respon by ID</title>
	<!-- styling -->
	<style>
		@import url('https://fonts.googleapis.com/css2?family=Handlee&family=Zen+Dots&display=swap');

		* {
			margin: 0;
			padding: 0;
		}

		body {
			background-image: url("/public/media/bg-night-2.webp");
			font-family: 'Zen Dots', cursive;
			background-position: center center;
			background-attachment: fixed;
			background-color: #22124F;
			background-size: cover;
		}

		.outter {
			position: absolute;
			backdrop-filter: blur(5px);
			width: 100%;
			height: 100%;
			padding: 1rem
		}

		.container {
			background-color: rgba(23, 10, 62, 0.4);
			box-shadow: 0 0 15px 10px #684C90;
			font-family: 'Handlee', cursive;
			backdrop-filter: blur(13px);
			border: solid 2px #d4ccdf;
			border-radius: 1em;
			transition: 1s;
			font-size: 1.2rem;
			color: #fff;
			max-width: 300px;
			min-height: 125px;
			max-height: 400px;
			padding: .7em;
		}

		.no-scrollbar {
			overflow: hidden;
		}

		.centered-text {
			text-align: center;
		}

		/* Responsive Viewport */
		@media (min-width: 576px) {
			.container {
				font-size: 1.7rem;
				max-width: 400px;
			}
		}

		@media (min-width: 768px) {
			.container {
				font-size: 2rem;
				max-width: 600px;
			}
		}
	</style>
</head>

<body>
	<div class="outter container-fluid d-flex justify-content-center align-items-center">
		<textarea class="inside d-block container no-scrollbar centered-text"><%= users.message %></textarea>
	</div>
	<!-- Script -->
	<script>
		function adjustTextareaHeight(textarea) {
			textarea.style.height = "";
			textarea.style.height = Math.min(textarea.scrollHeight, parseInt(getComputedStyle(textarea).maxHeight)) + "px";
		}
	</script>
</body>

</html>